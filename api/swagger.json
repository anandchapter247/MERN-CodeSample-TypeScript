{
  "swagger": "2.0",
  "info": {
    "title": "Custom apiDoc browser title",
    "version": "0.1.0",
    "description": "apiDoc basic example"
  },
  "paths": {
    "admin/login": {
      "post": {
        "tags": ["Admin"],
        "summary": "<p>In this case &quot;apiErrorStructure&quot; is defined and used. Define blocks with params that will be used in several functions, so you dont have to rewrite them.</p> ",
        "consumes": ["application/json"],
        "produces": ["application/json"],
        "parameters": [
          {
            "name": "email",
            "in": "parameter",
            "required": true,
            "type": "string",
            "description": "<p>Email of the Admin.</p> "
          },
          {
            "name": "password",
            "in": "parameter",
            "required": true,
            "type": "string",
            "description": "<p>Password of the Admin.</p> "
          },
          {
            "name": "isDeleted",
            "in": "parameter",
            "required": true,
            "type": "boolean",
            "description": "<p>If Admin is deleted then it will be true otherwise false.</p> "
          }
        ],
        "responses": {}
      }
    }
  },
  "definitions": {
    "PostAdmin": {
      "properties": {
        "email": {
          "type": "string",
          "description": "<p>Email of the Admin.</p> "
        },
        "password": {
          "type": "string",
          "description": "<p>Password of the Admin.</p> "
        },
        "isDeleted": {
          "type": "boolean",
          "description": "<p>If Admin is deleted then it will be true otherwise false.</p> "
        }
      },
      "required": ["email", "password", "isDeleted"]
    },
    "PostAdminSuccess": { "properties": {}, "required": [] }
  }
}
